# Next Cue

Next Cue is an ADHD routine companion for planning daily steps and finding a clear next action.

Generate the Xcode project with `xcodegen generate`. StoreKit testing is configured in the `NextCue` scheme through `NextCue.storekit`.

See `CLAUDE.md` for app identifiers and `.claude/rules/store-and-release.md` for store setup.
